"""
AI Quality Review Platform — REST API Application.

This package wraps the PR-Agent engine as a production-grade FastAPI service.
The PR-Agent engine (pr_agent/) is treated as a read-only third-party library
and is never modified.
"""
